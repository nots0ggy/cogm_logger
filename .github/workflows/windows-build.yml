name: Windows build

on:
    workflow_dispatch:
    push:
        tags:
            - 'v*'

permissions:
    contents: write

jobs:
    build:
        runs-on: windows-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: '3.11'

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 20

            - name: Install Python build dependencies
              shell: pwsh
              run: |
                  python -m pip install --upgrade pip
                  python -m pip install scapy pyinstaller

            - name: Build logger.exe with PyInstaller
              shell: pwsh
              working-directory: logger
              run: |
                  pyinstaller logger.spec -y
                  Write-Host "=== warn-logger.txt ==="
                  if (Test-Path "build\logger\warn-logger.txt") {
                      Get-Content "build\logger\warn-logger.txt" | Select-String -Pattern "src\.options","analyze"
                  }
                  Write-Host "=== xref analyze entries ==="
                  if (Test-Path "build\logger\xref-logger.html") {
                      Select-String -Path "build\logger\xref-logger.html" -Pattern "src\.options\.analyze","invalid" -SimpleMatch | Select-Object -First 10
                  }

            - name: Copy logger output into dist
              shell: pwsh
              run: |
                  New-Item -ItemType Directory -Force -Path "dist\cogm-logger\logger" | Out-Null
                  Copy-Item -Path "logger\dist\logger\*" -Destination "dist\cogm-logger\logger\" -Recurse -Force

            - name: Install UI dependencies
              shell: pwsh
              working-directory: ui
              run: npm ci

            - name: Install Neutralino CLI
              shell: pwsh
              run: npm install -g @neutralinojs/neu@11.3.1

            - name: Build Neutralino bundle
              shell: pwsh
              run: |
                  neu update
                  neu build

            - name: Stage update.bat into dist
              shell: pwsh
              run: |
                  if (Test-Path "update.bat") {
                      Copy-Item -Path "update.bat" -Destination "dist\cogm-logger\update.bat" -Force
                  }

            - name: Verify dist contents
              shell: pwsh
              run: |
                  Write-Host "=== dist/cogm-logger contents ==="
                  Get-ChildItem -Recurse "dist\cogm-logger" | Select-Object FullName, Length

            - name: Install Inno Setup
              shell: pwsh
              run: choco install innosetup -y --no-progress

            - name: Build installer with Inno Setup
              shell: pwsh
              run: |
                  & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" build-setup.iss
                  Write-Host "=== Output folder ==="
                  if (Test-Path "Output") { Get-ChildItem Output }

            - name: Zip portable build
              shell: pwsh
              run: |
                  Compress-Archive -Path "dist\cogm-logger\*" -DestinationPath "dist\cogm-logger-portable.zip" -Force

            - name: Upload portable zip
              uses: actions/upload-artifact@v4
              with:
                  name: cogm-logger-portable
                  path: dist/cogm-logger-portable.zip
                  if-no-files-found: error
                  retention-days: 30

            - name: Upload installer
              uses: actions/upload-artifact@v4
              with:
                  name: cogm-logger-installer
                  path: Output/cogm-logger-installer.exe
                  if-no-files-found: error
                  retention-days: 30

            - name: Create GitHub release on tag
              if: startsWith(github.ref, 'refs/tags/v')
              uses: softprops/action-gh-release@v2
              with:
                  files: |
                      dist/cogm-logger-portable.zip
                      Output/cogm-logger-installer.exe
                  draft: false
                  prerelease: false
